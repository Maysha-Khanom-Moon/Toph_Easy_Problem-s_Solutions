#include <bits/stdc++.h>
using namespace std;

int main() {
	int a,b;
	cin>>a>>b;

	int i = 0;

	while( a && b ){
        if((a%10 + b%10) > 9){
            i = 1;
            break;
        }
        a/=10;
        b/=10;
	}

	if(i) cout<<"Yes"<<endl;
	else cout<<"No"<<endl;

	return 0;
}
